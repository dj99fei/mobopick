<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/cross_fader_content2"
        >

        <ListView
            android:id="@id/list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>


        <RadioGroup
            android:id="@id/radio_group"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:orientation="horizontal">

            <RadioButton
                android:id="@id/exclusive"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="@drawable/bg_group_radio"
                android:button="@null"
                android:checked="true"
                android:gravity="center"
                android:minWidth="@dimen/width_group_radio"
                android:text="@string/exclusive"/>

            <RadioButton
                android:id="@id/all"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginLeft="@dimen/layout_inner_padding_horizontal_small"
                android:layout_weight="1"
                android:background="@drawable/bg_group_radio"
                android:button="@null"
                android:gravity="center"
                android:minWidth="@dimen/width_group_radio"
                android:text="@string/all"/>

        </RadioGroup>

    </RelativeLayout>

    <include layout="@layout/include_loading"/>
</RelativeLayout>